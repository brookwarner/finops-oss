# @visx/* still declares a React 16/17/18 peer range, but the repo runs React 19
# (the components work fine on 19). Default installs to legacy peer resolution so
# `npm install` / `npm ci` succeed without a manual --legacy-peer-deps flag, and
# local + Vercel + CI stay consistent.
legacy-peer-deps=true
